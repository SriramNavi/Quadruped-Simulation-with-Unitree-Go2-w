Mesh assets for robot-specific visual and collision geometry belong here.

The initial foundation uses primitive URDF geometry so the packages remain
buildable before vendor meshes or custom CAD exports are available.
